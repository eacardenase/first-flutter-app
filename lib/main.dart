import 'package:flutter/material.dart';

// import 'gradient_container.dart';
import 'package:first_app/gradient_container.dart'; // recommended

void main() {
  runApp(const MaterialApp(
    home: Scaffold(
      body: GradientContainer(),
    ),
  ));
}
